 import styled from 'styled-components';
 
 export const StyledFooter = styled.footer`
   footer {
     color: #828fa3;
     font-size: 12px;
     font-weight: 700;
     text-align: center;
     margin-top: 24px;

     a {
       text-decoration: none;
       color: #828fa3;
       &:hover {
         cursor: pointer;
       }
     }
   }
 `;
 
